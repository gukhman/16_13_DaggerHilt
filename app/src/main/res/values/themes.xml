<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme._16_13_DaggerHilt" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <!-- Primary dark branding color. -->
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <!-- Secondary color. -->
        <item name="colorAccent">@color/accentColor</item>
        <!-- Color for text on primary surfaces -->
        <item name="colorOnPrimary">@color/onPrimaryColor</item>
    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/onPrimaryColor</item>
        <item name="android:textColorSecondary">@color/onPrimaryColor</item>
    </style>

    <style name="Theme._16_13_DaggerHilt" parent="Base.Theme._16_13_DaggerHilt" />
</resources>